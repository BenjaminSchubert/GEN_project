# -*- mode: python -*-
#
# generated by :
#
# pyinstaller -y --clean --windowed --name phagocytes-auth \
#   --exclude-module _tkinter --exclude-module Tkinter --exclude-module enchant \
#   --onefile ../main.py

block_cipher = None


a = Analysis(['../../authentication_server/manage.py'],
             pathex=['../build'],
             binaries=None,
             datas=None,
             hiddenimports=[],
             hookspath=[],
             runtime_hooks=[],
             excludes=['_tkinter', 'Tkinter', 'enchant'],
             win_no_prefer_redirects=False,
             win_private_assemblies=False,
             cipher=block_cipher)

pyz = PYZ(a.pure, a.zipped_data,
             cipher=block_cipher)

exe = EXE(pyz,
          a.scripts,
          a.binaries,
          a.zipfiles,
          a.datas,
          name='phagocytes-auth',
          debug=False,
          strip=True,
          upx=True,
          console=True)
